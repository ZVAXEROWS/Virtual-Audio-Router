// =============================================================================
// EventDispatcher.cpp — Virtual Audio Router
// =============================================================================
// Implementation is fully in the header (template-based).
// This file exists to satisfy the CMake source list and may house
// non-template helper implementations in future phases.
// =============================================================================

#include "var/EventDispatcher.h"

namespace var {
// All implementation is in EventDispatcher.h (templates cannot be split).
// Future: add thread-safe async dispatch queue here (Phase 4+).
} // namespace var
